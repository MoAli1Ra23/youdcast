import 'package:flutter/material.dart';

class TryAppBar extends StatelessWidget {
  const TryAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}